# Day 2

## Activity 1

```javascript
// Arithmetic Operation

// Task 1 : Write a program to add a two numbers and log to the console 

const num1=20
const num2=10

console.log(`${num1} + ${num2} = ${(num1+num2)}`)

// Task 2 : Write a program to subtract two numbers and log to hte console 

console.log(`${num1} - ${num2} = ${(num1-num2)}`)

// Task 3 : Write a program to multiply two numbers and log to hte console 

console.log(`${num1} * ${num2} = ${(num1*num2)}`)

// Task 4 : Write a program to divide two numbers and log to hte console 

console.log(`${num1} / ${num2} = ${(num1/num2)}`)

// Task 5 : Write a program to find remainder when one number is divided by another number and log the result to the console 

console.log(`Remainder is = ${(num1%num2)}`)





```

## Activity 2

```javascript


// assignment operation

//task 6 : use the += operator  to add a a number to variable and log the result to console 

let number1=10

number1+=20
console.log(`${number1}`)

// task 7 : use the -= operator  to subtract a a number to variable and log the result to console 

let number2=30

 number2-=20
 console.log(`${number2}`);


```

## Activity 3

```javascript 

//task 4 

//comparison operators

const num1=10
const num2=20

console.log(`First Number : ${num1}`)
console.log(`Second Number : ${num2}`)
console.log();

// task 8 :program to compare two number using < and > and log the result to the console 

console.log(`${num1} < ${num2} : ${num1<num2}`)
console.log(`${num1} > ${num2} : ${num1>num2}`)
console.log();

// task 9 :program to compare two number using <= and >= and log the result to the console 

console.log(`${num1} <= ${num2} : ${num1<=num2}`)
console.log(`${num1} >= ${num2} : ${num1>=num2}`)
console.log();


// task 10c :program to compare two number using == adn === and log the result to the console 
    
console.log(`${num1} == ${num2} : ${num1==num2}`)
console.log(`${num1} === ${num2} : ${num1===num2}`)




```

## Activity 4

```javascript

//logical operation

const num1=10
const num2=10
const num3=10
const num4=20

console.log(`Number_1 : ${num1}`);
console.log(`Number_2 : ${num2}`);
console.log(`Number_3 : ${num3}`);
console.log(`Number_4 : ${num4}`);
console.log();

// task 11: 

if(num1==num2 && num2==num3)
    {
        console.log(`Number_1 is equal to Number_2 is equal to Number_3`);
    }
console.log();

// task 12:

if(num1==num2 || num1==num3)
    {
        console.log(`Number_1 is same as Number_2 or Number_3`);
    }
console.log();

// task 13:

if(num1!=num4)
    {
        console.log(`Number_1 is not equal to Number_4`);
    }
console.log();



```

## Activity 5

```javascript 5

// Ternary operator 

// task 14:

const num=10;

(num < 0) ? console.log(`Number is negative`) : console.log(`Number is Positive`);

```
